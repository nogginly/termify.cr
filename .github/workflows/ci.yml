name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, crystal: 1.18 }
          - { os: ubuntu-latest, crystal: 1.19 }
          - { os: ubuntu-latest, crystal: latest }
          - { os: macos-latest, crystal: 1.19 }
          - { os: macos-15-intel, crystal: 1.19 }
          - { os: windows-latest, crystal: 1.18 }

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: ${{matrix.crystal}}
      - name: Cache shards
        uses: actions/cache@v5
        with:
          path: |
            lib
            bin/ameba
          key: ${{ runner.os }}-shards-${{ hashFiles('**/shard.lock') }}

      # ---- Build debug release and check code quality on one platform

      - name: Install shards in dev mode
        if: ${{ runner.os == 'Linux' }}
        run: shards check || shards install

      - name: Setup debug environment (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: mkdir -p bin/debug

      - name: Build Debug (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: SHARDS_BIN_PATH=bin/debug shards build --debug

      - name: Check formatting (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: crystal tool format; git diff --exit-code src

      - name: Run static code analysis (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: bin/ameba src/

      - name: Run tests (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: crystal spec -Dtest

      # ---- Build releases on all platforms
      - name: Install shards for release mode
        if: ${{ runner.os != 'Linux' }}
        run: shards --production check || shards --production install

      # Non-Windows
      - name: Setup release environment
        if: ${{ runner.os != 'Windows' }}
        run: mkdir -p bin/release

      - name: Build Release
        if: ${{ runner.os != 'Windows' }}
        run: SHARDS_BIN_PATH=bin/release shards --production build --release

      - name: Run Sample `md2term`
        if: ${{ runner.os != 'Windows' }}
        run: 'bin/release/md2term 2>&1 | grep "Usage: md2term"'

      # Ah, Windows.
      - name: Setup release environment (Windows)
        if: ${{ runner.os == 'Windows' }}
        shell: powershell
        run: |
          mkdir bin -ErrorAction SilentlyContinue
          mkdir bin\release -ErrorAction SilentlyContinue

      - name: Build Release (Windows)
        if: ${{ runner.os == 'Windows' }}
        shell: powershell
        env:
          SHARDS_BIN_PATH: "bin/release"
        run: |
          shards --production build --release
